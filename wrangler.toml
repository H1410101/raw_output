name = "raw-output"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[vars]
ENVIRONMENT = "development"

# We will add D1 database bindings here in Checkpoint 3.2
[[d1_databases]]
binding = "DB"
database_name = "raw-output-db"
database_id = "PASTE_YOUR_DATABASE_ID_HERE" # Need to create this via npx wrangler d1 create
